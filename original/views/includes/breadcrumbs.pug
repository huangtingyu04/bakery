.card-breadcrumbs
    ul
        li
            a(href="index.html") 首頁
        -for (val in breadtag) {
            li.breadcrumb-item 
                a(href="javascript:;")= breadtag[val]
        -}